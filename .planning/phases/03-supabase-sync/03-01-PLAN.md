---
phase: 03-supabase-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - .env.local
autonomous: false
requirements:
  - PINS-13
user_setup:
  - service: supabase
    why: "Cloud pin storage — database URL and anon key required for sync hook to connect"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API Keys → anon public"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard → New project"
      - task: "Run the pins table migration SQL (provided in Plan 03)"
        location: "Supabase Dashboard → SQL Editor → New query"

must_haves:
  truths:
    - "@supabase/supabase-js is resolvable as an import"
    - "vitest can discover and run tests in the tests/ directory"
    - ".env.local contains NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
      contains: "include: ['tests/**/*.test.ts']"
    - path: ".env.local"
      provides: "Supabase credentials"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/"
      via: "include glob"
      pattern: "tests/\\*\\*/\\*\\.test\\.ts"
---

<objective>
Install @supabase/supabase-js and vitest, create vitest.config.ts, and set up .env.local with Supabase credentials obtained via a human checkpoint.

Purpose: Wave 1 infrastructure setup — the sync hook and merge tests both depend on this plan completing. Running in parallel with Plan 02 (pure merge logic).
Output: Package installs committed, vitest.config.ts exists, .env.local contains valid Supabase credentials.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-supabase-sync/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create vitest config</name>
  <read_first>
    - /home/wzrd/gwv2/gwv2/package.json (current deps to verify nothing conflicts)
    - /home/wzrd/gwv2/gwv2/tsconfig.json (check moduleResolution for vitest compat)
  </read_first>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <action>
    Run in /home/wzrd/gwv2/gwv2:

    ```bash
    npm install @supabase/supabase-js
    npm install -D vitest
    ```

    Then create vitest.config.ts at /home/wzrd/gwv2/gwv2/vitest.config.ts with this exact content:

    ```typescript
    import { defineConfig } from 'vitest/config';
    import path from 'path';

    export default defineConfig({
      test: {
        environment: 'node',
        include: ['tests/**/*.test.ts'],
        globals: true,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        },
      },
    });
    ```

    The `@` alias maps to the project root so test files can import `@/app/features/pins/sync/merge`.

    Also create the tests directory structure:
    ```bash
    mkdir -p /home/wzrd/gwv2/gwv2/tests/pins
    ```
  </action>
  <verify>
    <automated>cd /home/wzrd/gwv2/gwv2 && node -e "require('./node_modules/@supabase/supabase-js')" && echo "supabase-js OK" && npx vitest run --reporter=verbose 2>&1 | grep -E "(no test files|PASS|FAIL|Tests)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/@supabase/supabase-js` directory exists
    - `vitest.config.ts` exists and contains `include: ['tests/**/*.test.ts']`
    - `npx vitest run` exits without "Cannot find package 'vitest'" error
    - `tests/pins/` directory exists
  </acceptance_criteria>
  <done>Packages installed, vitest.config.ts written, tests/pins/ directory created.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Human provides Supabase project credentials</name>
  <read_first>
    - /home/wzrd/gwv2/gwv2/.env.local (check if already has Supabase vars)
  </read_first>
  <files>.env.local</files>
  <action>
    HUMAN ACTION REQUIRED — Claude cannot create a Supabase account or retrieve API keys.

    Steps for the user:
    1. Go to https://supabase.com/dashboard and sign in (or create a free account)
    2. Click "New project" — name it "groundwork-v2" or similar, choose a region close to you
    3. Wait for the project to finish provisioning (~1 minute)
    4. Go to Project Settings → API
    5. Copy "Project URL" → this is NEXT_PUBLIC_SUPABASE_URL
    6. Copy "anon public" key under "Project API Keys" → this is NEXT_PUBLIC_SUPABASE_ANON_KEY

    Add both to /home/wzrd/gwv2/gwv2/.env.local:
    ```
    NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
    ```

    Do NOT run the SQL migration yet — that comes in Plan 03.

    NOTE: .env.local is already gitignored by Next.js. Your credentials will not be committed.
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_SUPABASE" /home/wzrd/gwv2/gwv2/.env.local</automated>
  </verify>
  <resume-signal>Type "done" when .env.local has both Supabase vars, or paste the URL and key directly and I'll write .env.local for you.</resume-signal>
  <acceptance_criteria>
    - .env.local exists at /home/wzrd/gwv2/gwv2/.env.local
    - File contains a line matching: `NEXT_PUBLIC_SUPABASE_URL=https://`
    - File contains a line matching: `NEXT_PUBLIC_SUPABASE_ANON_KEY=`
    - `grep -c "NEXT_PUBLIC_SUPABASE" /home/wzrd/gwv2/gwv2/.env.local` returns 2
  </acceptance_criteria>
  <done>.env.local has both Supabase vars. App can initialize the supabase client without throwing.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -c "NEXT_PUBLIC_SUPABASE" /home/wzrd/gwv2/gwv2/.env.local` returns `2`
- `cat /home/wzrd/gwv2/gwv2/vitest.config.ts` shows `include: ['tests/**/*.test.ts']`
- `ls /home/wzrd/gwv2/gwv2/node_modules/@supabase/supabase-js/package.json` exits 0
- `ls /home/wzrd/gwv2/gwv2/tests/pins/` shows directory exists
</verification>

<success_criteria>
@supabase/supabase-js and vitest are installed, vitest.config.ts is configured with the @ path alias pointing at project root, and .env.local contains real Supabase project credentials. Plan 03 can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-supabase-sync/03-01-SUMMARY.md`
</output>
